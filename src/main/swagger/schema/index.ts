import { entryType } from './enum'
import { addEntryModelRequest, addEntryModelResponse, error, getAllEntryModelResponse } from './model'

const schemas = { entryType, addEntryModelRequest, addEntryModelResponse, error, getAllEntryModelResponse }

export { schemas }
